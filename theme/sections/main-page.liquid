<section class="sec sec-first">
  <div class="wrap">
    <span class="kicker rv">{{ shop.name }}</span>
    <h1 class="rv">{{ page.title }}</h1>
    <div class="rte rv d1" style="margin-top:1.8rem">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Seiteninhalt", "settings": [] }
{% endschema %}
